import React, { ReactElement } from 'react'

const LayoutLC = (children: ReactElement) => {

  return (
    <>
      <div>
        {/* Componentes */}
        {children}
      </div>
    </>
  )
}

export default LayoutLC
